version: '2'
services:

  mopidy:
    build:
      context: mopidy/
    command: mopidy --config /etc/mopidy/mopidy.conf
    ports:
      - "6600:6600"
      - "6680:6680"

  icecast:
    build:
      context: icecast/
    ports:
      - "8000:8000"
